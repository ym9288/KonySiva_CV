<Window x:Class="EmgucvDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmgucvDemo"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="1600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Image x:Name="img1"></Image>
            <Image x:Name="img2" Grid.Column="1"></Image>
            <Image x:Name="img3" Grid.Column="2"></Image>
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Click="OnReadImage">测试读取</Button>
        </StackPanel>
    </Grid>
</Window>
